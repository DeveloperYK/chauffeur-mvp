{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm run migrate:deploy && pnpm build",
  "crons": [
    {
      "path": "/api/clock-tick",
      "schedule": "* * * * *"
    }
  ]
}
