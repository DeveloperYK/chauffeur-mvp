/**
 * How long after a cancel the operator can take it back. Shared with the
 * console so the toast lives exactly as long as the server will honour Undo.
 */
export const UNDO_CANCEL_WINDOW_MS = 60_000;
